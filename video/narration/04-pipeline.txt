AArchTune replaces manual, speed-only tuning with a bounded pipeline. It detects the hardware, records a fixed baseline, creates a deterministic candidate plan, deduplicates equivalent low-level signatures, screens candidates, evaluates the real workload, checks quality and temporal drift, and only then ranks eligible configurations. Every decision remains attached to a hash-bound evidence trail.

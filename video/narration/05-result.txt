Here is the measured native result. The workflow completed one hundred thirty-two configured screening executions. Those became eleven distinct low-level signatures, and only four profiles advanced to expensive real-workload evaluation. Every advanced profile failed the unchanged quality policy, so the final outcome was no eligible candidate. That means no speedup claim and no deployment recommendation.

# Synthetic response fixtures

These records are hand-authored, deterministic test inputs. They are not model outputs,
benchmark evidence, or quality claims. Each file targets a specific validation path in
`workloads/smoke-test.jsonl`.

